name: Fetch FSA data

on:
  schedule:
    # JST 09:37（平日）＋ 予備 10:07 / 11:37 / 13:07
    # GitHubのcronは数時間遅延することがあるため多めに設定
    #（変更がなければコミットされないので多重実行は無害）
    - cron: '37 0 * * 1-5'
    - cron: '7 1 * * 1-5'
    - cron: '37 2 * * 1-5'
    - cron: '7 4 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: pip install --quiet openpyxl pypdf

      - name: Fetch FSA data
        run: python3 scripts/fetch_fsa.py

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add fund-transfer/registry fund-transfer/shobun
          if git diff --cached --quiet; then
            echo "No change"
          else
            git commit -m "Update FSA data (registry / shobun)"
            for i in 1 2 3; do
              git push && break
              git pull --rebase origin main
            done
          fi
